{% set name = "checkstyle" %}
{% set version = "10.17.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/checkstyle/{{ name }}/archive/{{ name }}-{{ version }}.tar.gz
  sha256: 2bd0bac4c268d4a5b95947a003081444c81115b763448d702bc41138bc7bd6c7
  folder: checkstyle

build:
  noarch: generic
  number: 0

requirements:
  build:
    - maven
    - openjdk
  run:
    - openjdk

test:
  commands:
    - {{ name }} --help

about:
  home: https://github.com/checkstyle/{{ name }}
  license: LGPL-2.1-or-later AND Apache-2.0
  license_file: 
    - checkstyle/LICENSE
    - checkstyle/LICENSE.apache20
    - checkstyle/target/generated-resources/licenses/
  summary: Checkstyle is a development tool to help programmers write Java code that adheres to a coding standard
  dev_url: https://github.com/checkstyle/{{ name }}

extra:
  recipe-maintainers:
    - danielnachun
