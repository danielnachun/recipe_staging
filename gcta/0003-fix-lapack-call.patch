From fef99c356069aac26b44a1f3f56aae08e867ad1d Mon Sep 17 00:00:00 2001
From: danielnachun <daniel.nachun@gmail.com>
Date: Sat, 30 Mar 2024 11:32:16 -0700
Subject: [PATCH 2/2] fix lapack call

---
 include/Matrix.hpp | 4 ++--
 src/StatLib.cpp    | 4 ++--
 2 files changed, 4 insertions(+), 4 deletions(-)

diff --git a/include/Matrix.hpp b/include/Matrix.hpp
index b203774..97dd9a1 100644
--- a/include/Matrix.hpp
+++ b/include/Matrix.hpp
@@ -32,7 +32,7 @@ bool _LLT(MatrixType &A, double &logdet){
 #if GCTA_CPU_x86
     dpotrf(&uplo, &cols, vi, &cols, &info);
 #else
-    dpotrf_(&uplo, &cols, vi, &cols, &info);
+    dpotrf_(&uplo, &cols, vi, &cols, &info, 1);
 #endif    
     //LOGGER << "  LLT time: " << LOGGER.tp("LLT") << std::endl;
     if(info == 0){
@@ -41,7 +41,7 @@ bool _LLT(MatrixType &A, double &logdet){
 #if GCTA_CPU_x86        
         dpotri(&uplo, &cols, vi, &cols, &info);
 #else
-        dpotri_(&uplo, &cols, vi, &cols, &info);
+        dpotri_(&uplo, &cols, vi, &cols, &info, 1);
 #endif
         //LOGGER << "  LLT inverse time: " << LOGGER.tp("LLT_INV") << std::endl;
         if(info == 0){
diff --git a/src/StatLib.cpp b/src/StatLib.cpp
index be56fb3..c22f3d1 100644
--- a/src/StatLib.cpp
+++ b/src/StatLib.cpp
@@ -115,10 +115,10 @@ namespace StatLib{
         char t = 'N';
 #if GCTA_CPU_x86
         dormqr(&side, &t, &n, &n, &n, X, &lda, tau, c, 
-                &lda, work, &lwork, &info);
+                &lda, work, &lwork, &info, 1, 1);
 #else
         dormqr_(&side, &t, &n, &n, &n, X, &lda, tau, c, 
-                &lda, work, &lwork, &info);
+                &lda, work, &lwork, &info, 1, 1);
 #endif
         if(info != 0){
             return false;
-- 
2.44.0

